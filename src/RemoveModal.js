/**
 *  This is a specialized class that asks the used if we wants to remove
 *  particular entity. If user say yes then it performs that removal and
 *  triggers 'done' event. Otherwise, it triggers 'cancel' event.
 */

module.exports = class {
};
